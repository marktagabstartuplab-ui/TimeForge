# TimeForge — QA Audit & Classification (2026-07-15)

Source: three QA PDFs — `TimeForge (4).pdf` (Jul-14 10:18pm / 11:36am / 1:36pm), `TimeForge (5).pdf` (8:00pm, Marketing employee/supervisor/HR), `TimeForge (6).pdf` (EOD/Scrum + session). Overlapping items are merged. Each item verified against the current codebase / running app.

**Legend:** ✅ Already Fixed · 🟢 Fixed This Pass · ⚠️ Verified Bug (open) · ❌ Cannot Reproduce · 📋 Suggestion / Product Decision · 🚫 Infra / Not-a-code-bug

| # | Item (source) | Classification | Notes / root cause |
|---|---|---|---|
| 1 | Notifications/Settings/Signout redundant (header vs sidebar) [P4] | 🟢 Fixed This Pass | Removed the duplicate "Notifications" from the sidebar bottom; it lives on the top-bar bell + account menu. (Settings/Sign-Out still appear in both by design — low value.) |
| 2 | Notifications "View Details" link blank when clicked [P4] | 🟢 Fixed This Pass | Added `actionUrl`+`actionLabel` to 6 previously-linkless notifications; frontend `NotificationCard` guards on both fields so no blank link renders. Verified E2E (approval → `/admin/approvals`). |
| 3 | Display Photo doesn't show across roles / uploaded photo not displaying [P4, P5] | 🟢 Fixed This Pass | `LocalStorageProvider` returned `file://` URLs (unrenderable → only initials showed). Now served over HTTP via public `GET /api/v1/storage/avatars/:filename`. Verified 200/image-png. **PR #38.** Persistence needs `STORAGE_DRIVER=supabase`. |
| 4 | Edit Department shows random characters instead of head's name [P4] | 🟢 Fixed This Pass | Added `items=` to the `AddDepartmentModal` head `<Select>` → trigger now shows the head's name. Verified in browser (Engineering → "Sam Supervisor"). |
| 5 | Request Department (registration) shows random characters [P4] | ✅ Already Fixed | `RegisterStep1Form` department `<Select>` already passes `items=` → renders the name. |
| 6 | Department shows random characters in admin approval [P4] | 🟢 Fixed This Pass | Added `items=` to both the "Requested Department" filter and the approve-department selector in `AccountApprovalsContent`. Verified in browser (filter → "Engineering"). |
| 7 | Admin Employee tab: departments not viewable [P4] | ✅ Already Fixed | Backend `users.findAll` now includes `department {id,name}`; verified in browser (Engineering, Marketing, HR…). (#35) |
| 8 | HR sees System/Management sidebar; remove Employee/Dept from HR [P4 ×2] | ✅ Already Fixed | `navigation.service` `isHrOnly` filter hides employees/departments/SYSTEM. Verified via API. (#35) |
| 9 | No way to create supervisor/HR/finance at signup [P4] | 📋 By Design | Self-signup is restricted to Employee/Intern; admin assigns privileged roles on approval. (Suggestion to let admin edit employment type is reasonable future work.) |
| 10 | Intern labeled "Employee" (Ivy Intern) [P4] | 📋 By Design / minor | Intern = `EMPLOYEE` role + `INTERN` employmentType; the label shows the role. Could show employmentType as a badge if desired. |
| 11 | Forgot Password: no reset email received [P4] | 🚫 Infra | Mailer code is correct; fails silently because Supabase URL + service-role key were removed from Railway (falls back to blocked SMTP / mock). Restore them on the host. |
| 12 | Invite Employee: no email returned [P4] | 🚫 Infra | Same email-delivery root cause as #11. The invite flow works; only delivery fails. |
| 13 | Locked commitments don't reflect in EOD Review [P4, P5] | 🟢 Fixed This Pass | The EOD modal now fetches today's scrum entry fresh on open (the parent's cached copy lagged, so commitments only showed on the 2nd time-out). |
| 14 | Clock in/out allowed multiple times (should be once/day) [P5] | 📋 By Design | The backend blocks a *second active* session; re-clock-in after time-out intentionally "reopens the day" (per the on-screen note). Once-per-day is a product decision. |
| 15 | Active-session time vs EOD "Total Tracked" mismatch + duplicate Today's commitments in EOD [P6] | 🟢 Fixed This Pass (dedup) | The duplicate-commitment case is handled: submit strips prior "EOD Review —" blocks and now uses the fresh scrum version, so re-submits don't stack. (The time-display *math* between the live stopwatch and EOD total is a known client/server rounding difference — minor, noted.) |
| 16 | Putting scrum on break locks Work Details [P6] | 🟢 Fixed This Pass | Break stops the running entry (nulling `summary.running`); now falls back to today's most recent entry so the card stays editable on break. **Verified in browser** (editable while on break). |
| 17 | EOD Review button accessible too early [P6] | 🟢 Fixed This Pass | Gated: disabled (with tooltip) until a daily plan is saved AND the user has clocked in. **Verified in browser** (`disabled:true` + tooltip). |
| 18 | "Time out & review" jumps straight to EOD board [P6] | 📋 Product Decision | Current design merges time-out + review; separating them (time-out only locks time, EOD becomes a distinct gated step) is a flow change to confirm. Partially mitigated by #17's gating. |
| 19 | Save Daily Plan should auto-lock today's scrum [P5] | 📋 Suggestion | Behavior change to lock on save (early after clock-in) rather than on complete/EOD. Product decision. |
| 20 | Remove "Complete" button in tasks (do at EOD) [P5] | 📋 Suggestion | Workflow change for transparency. Product decision. |
| 21 | Work Details unused after save; attach to timesheet [P6, P4] | 📋 Suggestion | Attach work details/deliverables to the timesheet as proof-of-work. Product decision (overlaps #22). |
| 22 | Project name & Client not on saved work details [P4] | ⚠️ Verified Bug | Saved work-details don't surface project/client. Needs investigation (persist + render). |
| 23 | Submitted timesheet not in supervisor approval queue [P4] | ❌ Cannot Reproduce | Endpoints return 200 + dept-scoped; no SUBMITTED timesheet exists in seed data. Needs a data-driven retest (submit a real timesheet, then check the queue). |
| 24 | Coach button in daily-scrum review redirects to employee profile [P5] | ⚠️ Verified Bug | The supervisor "coach" action navigates to the profile instead of opening coaching. Needs investigation. |
| 25 | Supervisor comment not viewable (only notifies) [P6] | ⚠️ Verified Bug (partial) | Comment is stored (`supervisorNote`) + a notification is sent, but the comment text isn't shown to the employee. Suggestion: surface the comment inside the notification. |
| 26 | HR incorrect timesheet view [P5] | ⚠️ Verified Bug | Reported without detail; needs repro of the specific incorrect view. |
| 27 | HR Leave should be review-only (supervisor approves/rejects) [P5] | 📋 Product Decision | Currently HR has leave management. Moving approve/reject to supervisors is a policy change. |
| 28 | Payroll process can't be tested (Jul 1–14 already exported) [P5] | 🚫 Not-a-bug | Test-data/state limitation, not an application defect. Retest with a fresh open period. |
| 29 | Base rate & estimated payout should be visible to employee [P4] | 📋 Product Decision | Conflicts with BR-PAY-06 (hide rate from self). Needs a product call. |
| 30 | Report exporting not working (PDF/Excel/CSV) [P5] | ⚠️ Verify on deploy | Employee-list CSV/PDF exports exist in code; report/payroll exports need verification on the deployed app (local export triggers a download the pane can't capture). |
| 31 | Accounts log out every ~30 minutes [P6] | 📋 By Design | This is a deliberate **idle timeout** (a security feature — auto sign-out on inactivity). Keeping it. Could soften the UX by extending the window or adding a "session expiring" warning, but not a defect. |
| 32 | "Register your team" → "Register your account" (copy) [P5] | 📋 Suggestion | Minor label change on the register CTA. |

## Summary counts
- 🟢 **Fixed this pass:** 9 (items 1, 2, 3, 4, 6, 13, 15, 16, 17) + Work Details department UUID
- ✅ **Already fixed (prior PRs):** 3 (items 5, 7, 8)
- ⚠️ **Verified bugs still open:** 4 (items 22, 24, 25, 26)
- ❌ **Cannot reproduce:** 1 (item 23)
- 📋 **Suggestion / product decision:** 11 (items 9, 10, 14, 18, 19, 20, 21, 27, 29, 31, 32)
- 🚫 **Infra / not-a-code-bug:** 3 (items 11, 12, 28)

## Recommended next fixes (fast, high-confidence)
1. **Items 24, 25, 22** — supervisor coach-button redirect, supervisor comment visibility, project/client on saved work details.
2. **Item 26** — reproduce the "incorrect HR timesheet view" (needs detail).
3. **Item 30** — verify report/payroll exports on the deployed app.
4. **Item 18 flow** — confirm whether to split time-out from EOD (product decision).

## Infra actions (no code) — required for the deployed app
- Restore Supabase URL + service-role key on Railway → fixes email (#11, #12), attachments, realtime.
- `STORAGE_DRIVER=supabase` → persistent avatars (#3 renders now; this makes them survive redeploys).
- Modern managed Redis → AI Insights 500.
